interface Props {
    type: React.HTMLInputTypeAttribute | 'textarea';
}

function CustomInput({ type }: Props): JSX.Element {
    // if (type === 'textarea') {
    // }

    return <div></div>;
}

export default CustomInput;
